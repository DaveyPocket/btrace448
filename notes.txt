Each component has std_logic_vector I/O type
Generic defined for integer and fraction portion of fixed point number


p_tick is one clock cycle long. Changes on rising edge of clock source.

Square_root_done on square root generator
